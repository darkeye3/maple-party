# 보스컷 랩 기획서

## 1. 목표

메이플스토리 캐릭터 닉네임과 헥사환산 한 값만 입력하면, 공식 캐릭터 정보와 비숍 기준 실전딜 곡선을 결합해 보스별 배율과 도전 상태를 빠르게 확인하는 도구를 만든다.

## 2. 대상 사용자

- 헥사환산은 알고 있지만 보스별 배율을 직접 계산하기 어려운 비숍 유저
- 파티 최소컷, 솔플 가능 구간을 한 화면에서 비교하려는 유저
- 캐릭터의 포스와 레벨 차이까지 반영된 대략적인 도전표가 필요한 유저

## 3. 핵심 흐름

1. 사용자가 닉네임과 헥사환산을 입력한다.
2. 서버가 NEXON Open API에서 직업, 레벨, 전투 스탯, 포스와 장비·어빌리티·하이퍼스탯·링크·유니온 프리셋을 조회한다.
3. 모든 프리셋 조합을 비숍 380% 방어율 보스 기준으로 비교해 가장 높은 실전딜 조합을 선택한다.
4. 비숍 기준 300/380 방어율 스플라인과 보스별 기준점을 이용해 피해량, 배율 및 카드 헥환을 계산한다.
5. 보스 카드를 배율순으로 표시하고 불가능, 파티컷, 솔플컷 상태를 구분한다.

## 4. 계산 기준

- 기준 캐릭터: 팸귄, 비숍, Lv.290, 아케인포스 1,360, 어센틱포스 620
- 기준 헥사환산: 83,583
- 보스별 배율 고정점: 사용자가 제공한 실제 Maplescouter 결과 화면 29개
- 헥환 변화: 확보한 `spline_300`, `spline_380`의 cubic Hermite 보간
- 300/380 변환: 비숍 실전 방무 계수와 두 방어율 피해량 비율 역산
- 카링 변환: 확보한 비숍 표본의 카링/380 피해량 고정 비율
- 최고 조건: 최대 810개 프리셋 조합을 스탯·공마·보공·방무·크뎀·크확·특수 반지 효율로 비교
- 레벨/포스 변화: 보스 레벨 및 요구 포스 대비 상대 배율

Maplescouter의 전체 원본 계산식은 공개 API가 아니다. 현재 엔진은 확보한 비숍 스플라인과 여러 프리셋 표본에서 방무·카링 계수를 역산한 모델이므로 **비숍 기준 추정치**다.

## 5. 데이터 및 구조

- 클라이언트: Next 호환 React 화면, shadcn UI, Tailwind CSS
- 서버 경로: `GET /api/character?nickname=...`
- 외부 데이터: NEXON Open API
- 공식 API 갱신 특성: 게임 데이터 반영까지 평균 약 15분
- 비밀값: 서버 환경변수 또는 사용자의 브라우저 세션에만 저장되는 `NEXON_API_KEY`
- 계산 엔진: `lib/model.ts`, `lib/presets.ts`

## 6. MVP 범위

- 닉네임 + 헥사환산 입력
- 공식 API 캐릭터 정보 조회
- 29개 보스 배율, 상태, 카드 헥환 표시
- 전체/도전권/솔플권 필터 및 정렬
- API 키가 없을 때 팸귄 기준 스냅샷 체험
- 사용자가 공식 API 키를 브라우저 탭에 연결해 다른 닉네임 조회
- 공식 API의 전투용 프리셋 전체 비교 및 최고 조건 자동 적용
- 300/380/카링 계산 피해량과 역산 헥환 표시
- 모바일 및 데스크톱 반응형 화면

## 7. 완료 조건

- 83,583 + 팸귄 입력 시 제공된 스크린샷 배율이 표시된다.
- 다른 헥환 입력 시 스플라인에 따라 배율과 카드 헥환이 함께 변한다.
- API 키 설정 시 닉네임으로 공식 캐릭터 정보가 조회된다.
- 조회 가능한 프리셋 조합 중 380% 방어율 실전딜이 가장 높은 조합이 선택된다.
- 헥환 81,271 기준 제공 표본의 300/380/카링 피해량을 반올림 오차 범위에서 재현한다.
- 프로덕션 빌드가 오류 없이 완료된다.

## 8. 다음 단계

- 직업별 실제 데이터 표본을 모아 직업 보정 계수 분리
- `calculatedHexaDamage_300/380/kaling` 실측 표본으로 오차 검증
- 보스 패치에 맞춘 요구 포스와 체력 데이터 버전 관리
- 사용자별 계산 이력 및 공유 링크
