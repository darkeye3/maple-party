# MapleParty | 게임 시스템 프로그래머 포트폴리오

> 전투 데이터를 보스 배율로 계산하고, 유저가 자신의 조건에 맞는 파티를 찾고 참가하도록 연결한 시스템 프로젝트입니다.

- 형태: 개인 프로젝트
- 기간: 2026.08 - 2026.09
- 담당: 기획, 데이터 분석, 계산 모델, 프론트엔드, 서버 도메인, 배포
- 기술: TypeScript, React 19, Cloudflare Workers, D1/SQLite, NEXON Open API
- [라이브 앱](https://boss-cut-lab.godnox3.chatgpt.site)
- [제출용 PDF](./output/pdf/MapleParty_Game_Programmer_Portfolio.pdf)

![MapleParty 보스 배율 화면](./output/pdf/screenshots/boss-rates.png)

## 프로젝트 목표

보스별 전투 배율을 보여주는 계산기에서 끝내지 않고 다음 판단을 한 흐름으로 만들었습니다.

1. 지금 도전할 수 있는 보스를 확인합니다.
2. 자신의 배율로 참가 가능한 파티를 찾습니다.
3. 역할별 최소컷, 남은 자리, 출발 시간과 보상 약정을 확인합니다.
4. 가입 요청 시 서버가 같은 계산 기준으로 조건을 다시 판정합니다.

```text
공식 캐릭터 데이터 + 사용자 HEXA 입력
                    ↓
         300% / 380% 전투 곡선
                    ↓
        레벨·포스·보스 기준점 보정
                    ↓
       보스 배율·도전 상태·파티 조건
```

## 핵심 구현

### 1. 전투력 환산 규칙 추출

메이플스토리 유저들이 캐릭터 전투력을 계산하던 엑셀 파일에는 직업, 장비, 보스 규칙이 33개 시트와 14,476개 수식 셀에 섞여 있었습니다. 전체 파일을 웹으로 옮기지 않고 파티 모집에 필요한 다음 경로만 추적했습니다.

```text
HEXA → 방어율별 피해량 → 레벨·포스 보정 → 보스 배율 → 도전 상태
```

300%와 380% 방어율 피해량은 각각 13개 제어점의 cubic Hermite spline으로 계산합니다. 피해량에서 표시 HEXA를 구할 때는 같은 곡선에 40회 이진 탐색을 적용합니다.

구현: [`lib/model.ts`](./lib/model.ts)

### 2. 프리셋 중복 적용 오류 수정

사용자가 입력하는 HEXA 환산값에는 선택한 장비, 어빌리티, 링크, 유니온 프리셋 효과가 이미 포함됩니다. 초기 모델은 탐색한 프리셋 상승량을 최종 피해량에 다시 곱해 결과를 부풀렸습니다.

프리셋 모듈은 최고 조건을 찾고 설명하는 책임만 갖도록 분리하고, 최종 피해량에는 같은 상승분을 재적용하지 않도록 수정했습니다.

구현: [`lib/presets.ts`](./lib/presets.ts), [`lib/model.ts`](./lib/model.ts)

### 3. 구버전 파티 배율 재사용 오류 수정

운영 중 하드 유피테르 파티 배율이 현재 입력 기준 `154.8%`가 아니라 과거 모델의 `2,290.0%`로 표시되는 문제를 확인했습니다.

원인은 DB에 저장한 파생값 `verified_rate`에 계산 모델 버전이 없어서, 모델이 바뀐 뒤에도 이전 값이 파티 합산에 사용된 것이었습니다.

- `verified_rate_version`을 추가해 계산 결과와 모델 버전을 함께 저장했습니다.
- 현재 버전과 다른 값은 파티 합산에서 제외합니다.
- 활성 참가자는 서버에서 다시 계산해 최신 값으로 교체합니다.
- 수정 후 하드 유피테르 파티 배율은 `154.8%`, 준비도는 `103%`로 정상화했습니다.

구현: [`lib/server/party-service.ts`](./lib/server/party-service.ts), [`lib/server/party-repository.ts`](./lib/server/party-repository.ts), [`db/schema.ts`](./db/schema.ts)

## 파티 모집 연결

![MapleParty 파티 모집 화면](./output/pdf/screenshots/party-board.png)

같은 보스 배율을 다음 위치에서 공통으로 사용합니다.

- 보스 카드의 도전 상태
- 가입 가능한 파티 필터
- 목표 배율 대비 현재 준비도
- 메인·보조 역할별 최소 배율
- 가입 요청의 서버 판정

파티 생성자는 목표 배율, 역할별 정원과 최소컷, 출발 시간, 보상 약정을 정의합니다. 참가자는 조건을 기억해 다른 화면에 옮겨 적지 않고 한 화면에서 지원 가능 여부를 확인합니다.

## 서버와 데이터 무결성

가입 요청은 다음 순서로 처리합니다.

1. 세션과 모집 상태를 확인합니다.
2. NEXON API에서 공식 캐릭터 프로필을 다시 조회합니다.
3. 입력 HEXA의 숫자 형식과 `0~250,000` 범위를 검사합니다.
4. 공식 조회값과 입력 HEXA로 보스 배율을 서버에서 계산합니다.
5. 역할 최소컷, 전체 정원, 역할별 좌석과 약정 버전을 확인합니다.
6. 계산 버전과 함께 참가 정보를 저장합니다.

D1/SQLite에는 `CHECK`, `UNIQUE`, `FOREIGN KEY`와 조회 인덱스를 적용했습니다. 중복 참가, 정원 초과, 조건 미달, 만료된 약정 동의를 애플리케이션과 저장 단계에서 방지합니다.

## 검증 범위

### 확인한 것

비숍 `팸귄`, HEXA `83,583`의 저장된 기준 스냅샷과 현재 엔진 결과를 비교했습니다.

| 항목 | 저장된 기준값 | 현재 엔진 | 상대 차이 |
|---|---:|---:|---:|
| 방어율 300% | 34.909824억 | 34.909824억 | `< 0.000001%` |
| 방어율 380% | 35.289770억 | 35.289770억 | `< 0.000001%` |
| 카링 조건 | 34.370663억 | 34.370663억 | `< 0.000001%` |

제어점 사이 6개 입력(`30k`, `50k`, `75k`, `100k`, `130k`, `150k`)에서는 정방향 계산 후 역산한 HEXA의 최대 오차가 `0`이었습니다.

이 결과는 **저장된 기준값의 재현성과 정방향·역방향 계산의 내부 일관성**을 확인한 것입니다. 독립된 외부 정답셋에 대한 정확도 검증은 아닙니다.

### 아직 증명하지 않은 것

- 독립 원본값을 사용한 여러 HEXA 구간의 오차
- 비숍 이외 직업의 정확도
- 패치 이후 변경된 보스 기준점의 자동 반영
- 사용자 입력 HEXA가 실제 캐릭터 값인지에 대한 확인

## HEXA 입력의 신뢰 경계

NEXON API에서 조회하는 직업, 레벨, 포스와 공개 스탯은 공식 데이터입니다. HEXA는 사용자가 직접 입력하며 현재 서버는 형식과 범위만 검사합니다.

서버가 보장하는 것은 **공식 조회값과 입력된 HEXA를 사용해 최종 배율을 다시 계산했다는 것**입니다. 입력 HEXA의 실제값이나 입력자의 캐릭터 소유권까지 증명하지 않으므로 참가자 상태는 `서버 재계산 완료`라는 범위로 표현합니다.

## 코드 인덱스

| 구분 | 파일 | 내용 |
|---|---|---|
| 전투 모델 | [`lib/model.ts`](./lib/model.ts) | Hermite spline, 역산, 방어율·레벨·포스 보정 |
| 프리셋 탐색 | [`lib/presets.ts`](./lib/presets.ts) | 장비·어빌리티·하이퍼·링크·유니온 비교 |
| 파티 서비스 | [`lib/server/party-service.ts`](./lib/server/party-service.ts) | 생성·가입·탈퇴와 서버 판정 |
| 파티 저장소 | [`lib/server/party-repository.ts`](./lib/server/party-repository.ts) | 버전별 배율 저장과 구버전 합산 제외 |
| 데이터 모델 | [`db/schema.ts`](./db/schema.ts) | D1 테이블, 제약 조건과 인덱스 |
| PDF 생성 | [`tools/build_portfolio_pdf.py`](./tools/build_portfolio_pdf.py) | 제출용 PDF 생성 스크립트 |

## 제출 포지셔닝

이 프로젝트는 TypeScript 기반 계산 모델링, 서버 도메인 설계와 라이브 운영 경험을 보여줍니다. C#/Unity 게임플레이 구현 역량은 별도 프로젝트인 **Abyss**와 함께 제시합니다.
